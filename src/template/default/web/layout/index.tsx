import React from "react";
import { Layout } from "@src/components";

const CurrentLayout = () => {
  return (
    <Layout
      Header={<div>Header</div>}
      Footer={<div>Footer</div>}
      Sidder={<div>Sidder</div>}
    />
  );
};

export default CurrentLayout;
